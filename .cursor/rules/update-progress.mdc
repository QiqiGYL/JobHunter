---
description: 功能改进时更新 progress.md，并带上日期
alwaysApply: true
---

# 更新项目进度 (progress.md)

当你完成**功能改进、新功能或重要修改**时，必须同步更新项目根目录的 **progress.md**。

## 必须做的两件事

1. **按时间记录**  
   在 `progress.md` 的 **「二、按时间记录（新在上）」** 部分**最顶部**新增一条：
   - 第一行格式：`- **YYYY-MM-DD**`（用**当天日期**）。
   - 下面 1～3 行写：本次改动了什么（功能/文件/行为），简洁即可。

2. **总览表（如需要）**  
   若新增了**新模块**或某模块**状态变化**（例如从「进行中」变为「完成」），在 **「一、进度总览（按模块）」** 的表格中更新对应行或新增一行。

## 示例

本次你做了：为 API 增加了「导出 Excel」接口。

则在「二、按时间记录」顶部添加：

```markdown
- **2026-02-15**
  - 后端新增 GET /api/jobs/export：返回当前职位列表的 Excel 下载。
```

若这是「导出」这一新模块的首次实现，在「一、进度总览」表格中增加一行，例如：

```markdown
| 职位导出 API | ✅ 完成 | GET /api/jobs/export 返回 xlsx |
```

这样用户可以通过 progress.md 按日期查看「哪天做了什么改变」。
